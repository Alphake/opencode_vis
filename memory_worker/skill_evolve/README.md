# memory_worker / skill_evolve

从 `vibetrace-skill` 迁入的**在线 skill 沉淀引擎**（不含 SWE-bench 评测）。

入口：`run_skill_evolve_pipeline`（由 `memory_worker/server.py` 的 `run_pipeline` 在 `MW_SKILL_PIPELINE=skill_evolve` 时调用）。

详见 [INTEGRATION_REPORT.md](./INTEGRATION_REPORT.md)。
