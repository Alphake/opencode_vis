# 2026-03-27 工作日志

## 上午架构决策讨论

### 确认的 opencode API 数据结构

**Session 对象**（`GET /session`）：
```json
{
  "id": "ses_2d79f7ff...",
  "slug": "mighty-tiger",
  "title": "New session - 2026-03-26T04:21:46.122Z",
  "directory": "D:\\projects\\opencode-dev\\opencode-dev",
  "version": "1.2.24",
  "summary": {"additions":0,"deletions":0,"files":0},
  "time": {"created":1774498906122,"updated":1774499250152}
}
```

**Todo 对象**（`GET /session/:id/todo`）：
```json
{
  "content": "了解 OpenCode 插件系统架构",
  "status": "pending|in_progress|completed",
  "priority": "high|medium|low"
}
```

### 三大架构决策（已定）

1. **不新建仓库**：继续在 `D:\projects\opencode_harness\agent-cockpit\agent-cockpit` 开发
2. **数据来源**：
   - MVP Demo（下周一）：只用 opencode 原生 API（`localhost:4096`），零额外依赖
   - 长期：加回 plugin + backend，提供历史数据和更丰富可视化
3. **前端样式**：无法复用 opencode SolidJS 组件（不是公开 npm 包），改为视觉参考 + React/Tailwind 重新实现

### 新前端架构（待实现）

布局：Header（session选择）+ 左侧 MessagePanel + 右侧 TodoPanel（D3卡片）

### 傍晚：cockpit-ui 开发完成（阶段1+2+3）

**确认的消息接口结构**（`GET /session/:id/message`）：
- 消息数组，每条 = `{ info: {...}, parts: [...] }`
- info 含 role/user/time/agent/model/tokens/finish
- parts 类型：text / reasoning / tool / step-start / text-file / image
- tool 含 callID / tool名 / state(status/input/output)

**新建 cockpit-ui 项目**：
- 位置：`cockpit-ui/`（Vite + React 19 + TS + Tailwind v4 + D3）
- CORS 验证：opencode 的 `localhost:4096` 完美支持 CORS，前端直连无问题
- 零后端依赖，直接 fetch opencode API

**已完成的组件**：
- `App.tsx`：主布局，左右两栏 + Header
- `Header.tsx`：Session 下拉选择器 + API 状态指示
- `MessagePanel.tsx`：左侧消息流列表 + 刷新按钮
- `MessageBubble.tsx`：单条消息气泡（区分 user/assistant）
- `ToolCallCard.tsx`：工具调用可折叠卡片（含 icon 映射）
- `ReasoningBlock.tsx`：思考过程折叠块（含时长）
- `TodoPanel.tsx`：右侧 Todo 列表 + 优先级/状态标签
- `EventFlowChart.tsx`：D3 横向矩形序列图（颜色映射事件类型）

**开发计划文档**：`docs/development-plan.md`（含完整 API 文档）
